import path from "path";
import { OUT_DIR_PATH } from "src/paths-config";

export const ROOT_PATH = path.join(__dirname, "root");
export const OUT_ROUTES_DIR_PATH = path.join(OUT_DIR_PATH, "routes");

